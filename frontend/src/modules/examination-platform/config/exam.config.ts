export const EXAM_PLATFORM_CONFIG = {
  sessionTimeoutMinutes: 180,
  enableAntiCheating: true,
  enableFullscreenLock: true,
  enableTabSwitchTracking: true,
  maxTabSwitchWarnings: 3,
  offlineSyncIntervalMs: 10000, // Sync answers queue every 10 seconds if network recovers
  allowedLateEntryMinutes: 15,
};
